# 智步 App 部署说明

## 📝 已完成的修改

✅ 所有 Perplexity API 调用已改为通过代理服务器
✅ 用户使用 App 无需翻墙
✅ 支持开发/生产环境自动切换

## 🚀 部署步骤

### 1. 启动代理服务器

```bash
cd api
npm start
```

服务器将在 `http://localhost:3000` 运行。

### 2. 配置 Sakura Frp 内网穿透

按照 [api/内网穿透使用说明.md](api/内网穿透使用说明.md) 配置：

1. 注册 Sakura Frp：https://www.natfrp.com/
2. 创建 HTTP 隧道，本地端口 3000
3. 启动隧道，获得公网域名（例如：`https://your-name.natfrp.cloud`）

### 3. 配置 App 的 API 地址

编辑 [lib/config.ts](lib/config.ts)：

```typescript
export const PERPLEXITY_PROXY_URL =
  __DEV__
    ? 'http://localhost:3000/api/chat'  // 本地开发
    : 'https://your-name.natfrp.cloud/api/chat';  // 👈 改成你的域名
```

**重要**：将 `your-name.natfrp.cloud` 改为你在 Sakura Frp 获得的实际域名。

### 4. 测试

#### 本地测试（开发环境）

```bash
# 终端 1：启动代理服务器
cd api
npm start

# 终端 2：启动 App
cd ..
npm start
```

在 App 中测试搜索功能，确保可以正常使用。

#### 生产测试（模拟用户使用）

```bash
# 1. 启动代理服务器
cd api
npm start

# 2. 启动 Sakura Frp
# （通过客户端或命令行）

# 3. 测试公网访问
curl -X POST https://your-name.natfrp.cloud/api/chat \
  -H "Content-Type: application/json" \
  -d '{"messages": [{"role": "user", "content": "你好"}]}'

# 4. 构建生产版本 App
npm run build  # 或 expo build
```

### 5. 发布 App

构建完成后，用户下载的 App 将自动通过你的代理服务器访问 Perplexity。

## 🔧 配置选项

### lib/config.ts

```typescript
// Perplexity 代理服务器地址
export const PERPLEXITY_PROXY_URL = '...';

// 是否使用代理（调试用）
export const USE_PERPLEXITY_PROXY = true;  // 改为 false 则直接调用 Perplexity
```

## 📱 使用场景

### 开发环境
- 开发者在本地开发时
- `__DEV__` 为 true
- 使用 `http://localhost:3000/api/chat`

### 生产环境
- 用户下载并使用 App 时
- `__DEV__` 为 false
- 使用你配置的公网域名

## ⚙️ 代理服务器维护

### 保持服务器运行

推荐使用 PM2：

```bash
npm install -g pm2
cd api
pm2 start npm --name "perplexity-proxy" -- start
pm2 startup  # 开机自启
pm2 save
```

### 查看日志

```bash
pm2 logs perplexity-proxy
```

### 重启服务

```bash
pm2 restart perplexity-proxy
```

## 🔒 安全注意事项

1. **保护 API Key**
   - `.env` 文件已在 `.gitignore` 中
   - 不要将 API Key 提交到 Git

2. **限制访问**（可选）
   - 可以在代理服务器添加简单的认证
   - 或使用 IP 白名单

3. **监控使用量**
   - 定期检查 Perplexity API 使用量
   - 避免被滥用

## 🆘 常见问题

### Q: App 提示"请求失败"
**A**:
1. 检查代理服务器是否运行：`curl http://localhost:3000/health`
2. 检查 Sakura Frp 隧道是否启动
3. 检查 `lib/config.ts` 中的域名是否正确

### Q: 开发环境正常，生产环境失败
**A**:
1. 确认已修改 `lib/config.ts` 中的生产环境域名
2. 测试公网域名是否可访问
3. 检查 CORS 配置（已在代理服务器启用）

### Q: 需要切换回直接调用 Perplexity
**A**:
修改 `lib/config.ts`：
```typescript
export const USE_PERPLEXITY_PROXY = false;
```

## 📊 架构说明

```
用户 App (React Native)
    ↓
lib/perplexityClient.ts (根据配置选择)
    ↓
    ├─→ 代理模式: http://your-name.natfrp.cloud/api/chat
    │       ↓
    │   api/server.js (你的 Mac)
    │       ↓
    │   https://api.perplexity.ai (Perplexity 官方)
    │
    └─→ 直接模式: https://api.perplexity.ai (需要 VPN)
```

## ✅ 检查清单

部署前确认：

- [ ] 代理服务器正常运行
- [ ] Sakura Frp 隧道已启动
- [ ] 获得固定公网域名
- [ ] 修改 `lib/config.ts` 中的域名
- [ ] 测试公网访问正常
- [ ] App 构建完成
- [ ] 用户可以正常使用搜索功能

---

**完成以上步骤后，你的 App 就可以让国内用户无需 VPN 使用了！** 🎉
